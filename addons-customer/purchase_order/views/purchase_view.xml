<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- view tree ของ Requests for Quotation (RFQ) -->
    <record id="purchase.purchase_order_kpis_tree" model="ir.ui.view">
        <field name="name">purchase.order.inherit.purchase.order.tree</field>
        <field name="model">purchase.order</field>
        <field name="priority" eval="10"/>
        <field name="arch" type="xml">
            <tree string="Purchase Order" multi_edit="1" class="o_purchase_order"
                    js_class="purchase_list_dashboard" sample="1">
                <header>
                    <button name="action_create_invoice" type="object" string="Create Bills"/>
                    <button name="select_create_purchase" type="object" string="Create PO"/>
                </header>
                <field name="priority" optional="show" widget="priority" nolabel="1"/>
                <field name="partner_ref" optional="hide"/>
                <field name="name" string="Reference" readonly="1" decoration-bf="1"/>
                <field name="date_approve" invisible="context.get('quotation_only', False)" optional="show"/>
                <field name="partner_id" readonly="1"/>
                <field name="company_id" readonly="1" options="{'no_create': True}"
                    groups="base.group_multi_company" optional="show"/>
                <field name="date_planned" invisible="context.get('quotation_only', False)" optional="show"/>
                <field name="user_id" optional="show" widget="many2one_avatar_user"/>
                <field name="date_order" attrs="{'invisible': ['|', '|', ('state', '=', 'purchase'), ('state', '=', 'done'), ('state', '=', 'cancel')]}"
                    invisible="not context.get('quotation_only', False)" widget="remaining_days" optional="show"/>
                <field name="activity_ids" widget="list_activity" optional="show"/>
                <field name="analytic_account_id" optional="show"/>
                <field name="internal_detail" optional="show"/>
                <field name="origin" optional="show"/>
                <field name="amount_untaxed" sum="Total Untaxed amount" string="Untaxed" widget="monetary" optional="hide"/>
                <field name="amount_total" sum="Total amount" widget="monetary" optional="show" decoration-bf="1"/>
                <field name="currency_id" invisible="1"/>
                <field name="state" optional="show" widget="badge" decoration-success="state == 'purchase' or state == 'done'"
                    decoration-warning="state == 'to approve'" decoration-info="state == 'draft' or state == 'sent'"/>
                <field name="invoice_status" optional="hide"/>
            </tree>
        </field>
    </record>


    <record id="view_purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <header>
                <button string="Take Price from Tender" name="button_update_tender" type="object" attrs="{'invisible': [('requisition_id', '=', False)]}"/>
            </header>
            <xpath expr="//field[@name='requisition_id']" position="after">
                <field name="analytic_account_id"/>
                <field name="internal_detail"/>
            </xpath>
            <xpath expr="//field[@name='order_line']/tree//field[@name='name']" position="replace">
                <field name="name" optional="show"/>
                <field name="product_variant" string="Product Variant" widget="many2many_tags" options="{'no_create_edit': True}" class="oe_inline" optional="show"/>
                <field name="product_name" string="Vendor Product Name" class="oe_inline" force_save="1" optional="show"/>
                <field name="product_code" string="Vendor Product Code" class="oe_inline" force_save="1" optional="show"/>
            </xpath>
            <xpath expr="//field[@name='order_line']/tree//field[@name='product_uom']" position="after">  
                <field name="secondary_product_uom" string="P UoM" 
                                        attrs="{
                                            'readonly': [('state', 'in', ('purchase', 'done', 'cancel'))],
                                            'required': [('display_type', '=', False)]
                                        }"
                                        force_save="1" optional="show"/>
                <field name="p_quantity" string="P Quantity" class="oe_inline" optional="show"/>
                <field name="p_unit_price" string="P Unit Price" class="oe_inline" force_save="1" attrs="{'readonly': [('type_product', 'not in', ['aluminium', 'aluminium sheet', 'glass'])]}" optional="show"/>
                <field name="density" attrs="{
                                            'readonly': [('type_product', '!=', 'aluminium')],
                                        }" optional="show"/>
                <field name="price_unit_before_discount" readonly="1" optional="show"/>
                <field name="discount" string="Discount %" optional="show"/>
                <field name="type_product" invisible="1"/>
            </xpath> 
            <xpath expr="//field[@name='order_line']/tree//field[@name='price_unit']" position="attributes">
                <attribute name="optional">show</attribute>
            </xpath>
            <xpath expr="//field[@name='order_line']/tree//field[@name='product_qty']" position="attributes">
                <attribute name="attrs">{'readonly': [('state', 'in', 'purchase')]}</attribute>
            </xpath>
            <xpath expr="//field[@name='currency_id']" position="after">
                <!-- <field name="check_by_id"/>
                <field name="approve_1_by_id"/>
                <field name="approve_2_by_id" attrs="{'invisible': [('check_config_po', '=', False)]}"/> -->
                <field name="check_config_po" invisible="1"/>
                <field name="hide_check_1" invisible="1"/>
                <field name="hide_check_2" invisible="1"/>
                <field name="hide_check_3" invisible="1"/>
            </xpath>
            <xpath expr="//button[@name='print_quotation']" position="after">
                <button name="button_check_1" string="Check 1" type="object" attrs="{'invisible': [('hide_check_1', '=', True)]}"/>
                <button name="button_approve_1" string="Check 2" type="object" attrs="{'invisible': [('hide_check_2', '=', True)]}"/>
                <button name="button_approve_2" string="Check 3" type="object" attrs="{'invisible': [('hide_check_3', '=', True)]}"/>
            </xpath>
            <xpath expr="//field[@name='picking_type_id']" position="after">
                <field name="delivery_address"/>
            </xpath>
        </field>
    </record>


    <record id="search_purchase_order_view_tree" model="ir.ui.view">
        <field name="name">search.purchase.order.view.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
        <field name="arch" type="xml">
            <search>
                <field name="internal_detail"/>
            </search>
        </field>
    </record>

    <!-- view tree ของ Purchase Order -->
    <record id="purchase.purchase_order_view_tree" model="ir.ui.view">
        <field name="name">purchase.order.view.tree</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <tree decoration-muted="state=='cancel'"
                decoration-info="state in ('wait','confirmed')"
                string="Purchase Order"
                class="o_purchase_order"
                sample="1">
                <header>
                    <button name="action_create_invoice" type="object" string="Create Bills"/>
                </header>
                <field name="priority" optional="show" widget="priority" nolabel="1"/>
                <field name="partner_ref" optional="hide"/>
                <field name="name" string="Reference" readonly="1" decoration-bf="1"/>
                <field name="date_approve" widget="date" invisible="context.get('quotation_only', False)" optional="show"/>
                <field name="partner_id"/>
                <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="show"/>
                <field name="date_planned" invisible="context.get('quotation_only', False)" optional="show"/>
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="analytic_account_id" optional="show"/>
                <field name="internal_detail" optional="show"/>
                <field name="date_order" invisible="not context.get('quotation_only', False)" optional="show"/>
                <field name="activity_ids" widget="list_activity" optional="show"/>
                <field name="origin" optional="show"/>
                <field name="amount_untaxed" sum="Total Untaxed amount" string="Untaxed" widget="monetary" optional="hide"/>
                <field name="amount_total" sum="Total amount" widget="monetary" optional="show" decoration-bf="1"/>
                <field name="currency_id" invisible="1"/>
                <field name="state" invisible="1"/>
                <field name="invoice_status" widget="badge" decoration-success="invoice_status == 'invoiced'" decoration-info="invoice_status == 'to invoice'" optional="show"/>
            </tree>
        </field>
    </record>

</odoo>