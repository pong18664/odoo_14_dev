<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- แทรกปุ่ม smart button 'Delivery' -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_doc_delivery" type="object" class="oe_stat_button" icon="fa-inbox" 
                    attrs="{'invisible': [('count_doc_delivery', '=', 0)]}">
                    <field name="count_doc_delivery" string="Delivery" widget="statinfo"/>
                </button>
            </xpath>

            <!-- check box สำหรับระบุ po type subcontract -->
            <xpath expr="//field[@name='fiscal_position_id']" position="after">
                <field name="po_type_subcontract"/>
            </xpath>

            <!-- แทรก field ใน order_line -->
            <xpath expr="//field[@name='order_line']/tree//field[@name='name']" position="after">
                <field name="variant_bom_id"
                    domain="[
                        ('type', '=', 'subcontract'),
                        ('product_id', '=', product_id),
                        ]"
                    options="{'no_create': True}"/>
            </xpath>

        </field>
    </record>
    

</odoo>